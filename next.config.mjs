/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "api.microlink.io",
        '127.0.0.1', // Microlink Image Preview
      ],
    },
  };

export default nextConfig;
